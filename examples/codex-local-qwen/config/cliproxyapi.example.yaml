host: "127.0.0.1"
port: 8319
auth-dir: "~/.cli-proxy-api"

# This protects the local endpoint. Keep the same value in config.toml.
api-keys:
  - "sk-local-codex"

debug: false
logging-to-file: true
logs-max-total-size-mb: 100
request-retry: 1
routing:
  strategy: "fill-first"
